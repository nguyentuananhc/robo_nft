import React from 'react'

const TimeLineCardTime = ({ children }) => {
  return (
    <div className="font-primary text-base font-semibold leading-body text-primary">
      {children}
    </div>
  )
}

export default TimeLineCardTime
